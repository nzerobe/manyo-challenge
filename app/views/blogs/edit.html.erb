
  <%= form_with(model: @blogs, local: true) do |form| %>
<div class="blog_title">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>
  <div class="blog_title">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>
  <h2>Create a new Post</h2>

  
  <%= form.submit %>
<% end %>
        <%= link_to 'このタスクの詳細', @blog, class:"btn btn-lg btn-secondary btn-block" %>
        <%= link_to 'タスク一覧画面', blogs_path, class:"btn btn-lg btn-secondary btn-block" %>
  
